#include<stdio.h>
int  RecFact(int n)
{
    return (n==1)?n:n*RecFact(n-1);
}

int main()
{
    int n;
    printf("Enter an integer number:");
    scanf("%d",&n);
    printf("Factorial:%d\n",RecFact(n));
}
